# 🚀 Система обмена сообщениями Raspberry Pi ↔ STM32 ↔ Arduino

---

## 📋 Changelog

### 📅 Версия 1.0.0 | 1 ноября
- **Первоначальный релиз**
- Реализована базовая архитектура взаимодействия
- Настроена коммуникация Raspberry Pi → STM32 через UART
- Реализовано управление Arduino через I2C шину
- Добавлена система кодов ошибок и подтверждений
- Поддержка 9 зон управления

### 📅 Версия 1.0.2 | 10 ноября
- реализован ответ в виде json файла
- функция была обернута в класс для последущих изменений
- немного подредактирован код стмки
- обновлена документация

---

## 📘 Описание проекта

Проект реализует взаимодействие между **Raspberry Pi**, **STM32** и несколькими **Arduino**, подключёнными по шине **I2C**.
Raspberry Pi отправляет команды STM32 через **USART**, STM32 принимает их и пересылает нужной Arduino по **I2C**, а Arduino выполняет команду и возвращает подтверждение.

Система поддерживает девять зон управления (по одной Arduino на каждую зону).
Каждая Arduino имеет свой I2C-адрес, а STM32 выступает в роли мастера шины.

---

## ⚙️ Пример использования

### На Raspberry Pi:

```python
send_command_to_stm32(arduino_number, scenario_number)
send_command_to_stm32(3, 0x05)
```

### Возвращаемые данные:
Возвращает словарь 

```python
{
    "number_of_command": Номер выполняемой команды,
    "error_code": Код ошибки или 0x00 если ошибки нет,
    "command_status": "OK"/"Failed", 
    "execution_time_ms": Время выполнения команды или 0 если проблема с передачей
}
```

---

## ⚡ Общая схема работы

```
┌────────────────────────┐
│     Raspberry Pi        │
│  (отправка по UART)     │
└──────────┬──────────────┘
           │ USART
┌──────────▼──────────────┐
│         STM32           │
│ (приём, обработка, I2C) │
└──────────┬──────────────┘
           │ I2C-шина
┌──────────▼──────────────┐
│        Arduino #1        │ (Зона 1, 0x21)
├──────────────────────────┤
│        Arduino #2        │ (Зона 2, 0x22)
├──────────────────────────┤
│        ...               │
├──────────────────────────┤
│        Arduino #9        │ (Зона 9, 0x29)
└──────────────────────────┘
```

---

## 🧩 Принцип обмена сообщениями

### 📤 1. Raspberry Pi → STM32 (через UART)

Сообщение состоит из **2 байт**:

| Байт | Описание         | Пример |
|------|------------------|--------|
| 1    | Номер зоны (1–9) | `0x03` |
| 2    | Номер команды    | `0x15` |

---

### 🔄 2. STM32 → Arduino (через I2C)

STM32 получает данные, определяет адрес нужной Arduino и пересылает **1 байт — команду** на её I2C-адрес.

Адрес рассчитывается так:

```
0x20 + номер_зоны
```

| Зона | Адрес Arduino |
|------|---------------|
| 1    | `0x21`        |
| 2    | `0x22`        |
| ...  | ...           |
| 9    | `0x29`        |

---

### 📬 3. Ответы и коды ошибок

После выполнения команды Arduino отправляет 1 байт-ответ STM32, который пересылает его Raspberry Pi.

| Код   | Значение                                    |
|-------|---------------------------------------------|
| `0x00`| Подтверждение (успешно)                     |
| `0x01`| Ошибка передачи на Arduino                  |
| `0x02`| Ошибка подтверждения от Arduino             |
| `0x03`| Неверный адрес Arduino (зона вне диапазона) |
| `0x04`| Ошибка связи с STM32 |

---

## 📦 Структура репозитория

```
├── rasbery_py.py        # Код для Raspberry Pi (UART)
├── stm.c                # Код для STM32 (UART + I2C)
├── arduino_temp.cpp     # Код для Arduino (I2C slave)
├── README.md            # Описание проекта
└── /docs                # Документация, схемы (в будущем)
```

---

## 🚀 Возможные улучшения

### 🔧 Планируемые функции:

- [ ] **Удобная реализация сценариев**: создание таблицы в формате:
  ```
  Номер сценария | Номер ардуино (1-9) | Описание сценария
  ```

- [ ] **Функция-обертка**: автоматическое определение адреса Arduino из таблицы
  - Для запуска сценария потребуется только 1 аргумент (номер сценария)
  - Снижение вероятности ошибок
  - Уменьшение количества кода

---

## 👥 Авторы 

**Автор**: *Сергеев А.М / киберполигон*

---
